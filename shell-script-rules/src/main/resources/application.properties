# Default application properties
server.port=8080
app.environment=development
app.message=Welcome to PetClinic!

# Actuator endpoints
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
